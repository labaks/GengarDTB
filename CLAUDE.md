# deskos — настольный помощник на ESP32 (CYD)

> Файл памяти проекта. Читать в начале каждой сессии. Обновлять при изменении решений.

## Быстрый старт сессии

```powershell
# окружение (нужно в каждой новой оболочке)
powershell -ExecutionPolicy Bypass -NoProfile -NoExit -File C:\esp\esp-idf\export.ps1

# сборка
idf.py build

# прошивка + лог загрузки, порт определяется сам
powershell -ExecutionPolicy Bypass -File tools\flash.ps1
powershell -ExecutionPolicy Bypass -File tools\flash.ps1 -Log 30

# хостовые тесты (datasource_resolve/render), без ESP-IDF и без железа —
# требуют окружение ESP-IDF активированным (для IDF_PATH → cJSON) и MinGW gcc,
# см. «Хостовые тесты» ниже
cmake -S tests\datasource -B tests\datasource\build -G Ninja -DCMAKE_C_COMPILER=C:/mingw64/bin/gcc.exe
cmake --build tests\datasource\build
ctest --test-dir tests\datasource\build --output-on-failure
```

`idf.py monitor` **не использовать в скриптах** — он не завершается. Для этого есть
`tools/monitor.py`, снимающий лог за фиксированное окно и корректно отпускающий
линии DTR/RTS.

Дальше по порядку: план работ в [ROADMAP.md](ROADMAP.md), формат приложений в
[docs/app-format.md](docs/app-format.md).

**Правило коммитов:** тайтл начинается с номера задачи ROADMAP, например
`#18. Apps: rescan + delete from device, no reboot`.

## Что строим

Настольное устройство-помощник: ESP32 + дисплей + кнопки, постоянно под питанием на столе.
Оболочка устройства работает как **мини-ОС**: лончер, список установленных виджетов,
пользователь сам добавляет/удаляет приложения-виджеты (погода, курсы валют, напоминания)
**без перепрошивки и без компиляции**.

Терминология: это не ОС в строгом смысле (планировщик уже есть — FreeRTOS внутри ESP-IDF).
Мы строим **shell + app runtime + app store**. Настоящий продукт здесь — стабильный API
виджета и формат приложения, а не оболочка сама по себе.

## Железо (проверено на живой плате, не из даташита)

Плата: **ESP32-2432S028R** («Cheap Yellow Display», CYD), 2.8" 320x240.

| Параметр | Значение |
|---|---|
| Чип | ESP32-D0WD-V3, **revision v3.1**, dual-core Xtensa LX6, 240 МГц |
| Flash | **4 МБ** (Boya, mfr 0x68, dev 0x4016), 3.3 В по strapping |
| PSRAM | **НЕТ** (~300 КБ свободной heap после LVGL+WiFi+TLS) |
| MAC | `44:1d:64:f3:39:0c` |
| USB | CH340 (VID 1A86 / PID 7523) → **COM13** |

### Распиновка (подтверждена по двум источникам)

| Периферия | Пины |
|---|---|
| Дисплей ILI9341 | SPI2/HSPI: SCK 14, MOSI 13, MISO 12, CS 15, DC 2, RST — нет, подсветка **21** (LEDC) |
| Тач XPT2046 | CLK 25, MOSI 32, MISO 39, CS 33, IRQ 36 |
| microSD | SCK 18, MOSI 23, MISO 19, CS 5 |
| RGB LED | R 4, G 16, B 17 — **активный низкий уровень** |
| LDR (фоторезистор) | 34 |
| Динамик | 26 |
| **Свободные GPIO** | **22, 27, 35** — и всё. GPIO35 только вход, **без внутренних pull-up** |

### ⚠️ Конфликт SPI-шин — важнейшее архитектурное ограничение платы

Тач (25/32/39/33) и SD (18/23/19/5) формально оба «VSPI», но с **разными пинами**.
Один SPI-контроллер не обслуживает два набора пинов; доступных шин у ESP32 всего две.
SDMMC-периферия не спасает — её слот-пины (14/15/2) конфликтуют с дисплеем.

**Принятое решение:** SPI2 → дисплей, SPI3 → SD (ей нужна пропускная способность),
тач → **программный SPI (bitbang)**, свой драйвер в `components/bsp/bsp_touch.c`.
XPT2046 тихоходный (~2 МГц), bitbang для него достаточен.
Побочный плюс: не нужен сторонний компонент `esp_lcd_touch_xpt2046`.

### ⚠️ CH340 и авто-ресет — почему хост-канал по WiFi, а не по USB

На плате штатная схема авто-ресета через DTR/RTS (ею пользуется esptool). Любая программа,
открывающая COM13, **перезагружает ESP32**. При уходе ПК в сон и особенно при выходе из сна
USB пере-энумерируется → устройство будет ребутиться каждое утро.

**Решение:** канал «устройство ↔ ПК-агент» — по **WiFi** (mDNS + WebSocket).
USB-serial только для прошивки, логов и первичной настройки. Не боевой транспорт.

## Бэкап заводской прошивки

На плате была **чужая** прошивка погодной станции (пути сборки указывают на машину
пользователя `ageitgey`, то есть был залит готовый чужой бинарник). Репозиторий не найден.

- Полный дамп: `D:\Downloads\cyd-flash-backup\cyd_full_4MB_44-1d-64-f3-39-0c.bin`
- 4 194 304 байта, SHA-256 `D8BEA5AEEB83FC5E0EE54927BC5F738BCA87BE1E0D61EF13D646A4EED01450C7`
- Разбор по партициям: `D:\Downloads\cyd-flash-backup\parts\`
- ⚠️ **Содержит NVS с SSID и паролем WiFi.** Публично не выкладывать.

Восстановление:
```
python -m esptool --port COM13 --baud 921600 write-flash --flash-size keep 0x0 "D:\Downloads\cyd-flash-backup\cyd_full_4MB_44-1d-64-f3-39-0c.bin"
```

Опознанный стек той прошивки (для справки): Arduino-ESP32 core 3.2.0 на IDF 5.4.1,
LVGL 9.x, WiFiManager 2.0.17 (tzapu, captive portal на 192.168.4.1), Open-Meteo
(`/v1/forecast` + geocoding-поиск города по имени), SNTP `pool.ntp.org`.
Заводская разметка была Arduino `huge_app`: один app0 3 МБ + spiffs 896 КБ (spiffs пуст),
второго OTA-слота не было. Coredump содержал 18.9 КБ — прошивка падала.

## Окружение сборки

- ESP-IDF **v5.5.5** в `C:\esp\esp-idf`, тулчейн в `C:\Users\labax-home\.espressif`
- `xtensa-esp-elf-gcc` 14.2.0, CMake 3.30.2, Ninja 1.12.1, Python 3.13.7
- Проект: `C:\proj\deskos` — короткий путь без пробелов **обязателен** (IDF на Windows)
- esptool 5.1.0 доступен глобально как `python -m esptool`

Активация окружения (нужна в каждой новой сессии оболочки):
```
powershell -ExecutionPolicy Bypass -NoProfile -NoExit -File C:\esp\esp-idf\export.ps1
```

Почему 5.5.5, а не 6.0: линия 5.5 — зрелая стабильная с лучшей совместимостью сторонних
компонентов (LVGL-порт, esp_lcd). v6.0 слишком свежая, часть компонентов не адаптирована.

### Хостовые тесты (`tests/datasource`)

ESP-IDF на Windows ставит только кросс-компилятор `xtensa-esp-elf-gcc` — для хоста
компилятора нет вообще (ни `gcc`, ни `cl`, ни `clang`; WSL без установленных
дистрибутивов тоже не считается). Поставлен отдельно: **MinGW-w64 (WinLibs,
POSIX/UCRT)**, распакован в `C:\mingw64`. Ставился вручную с winlibs.com —
`winget install BrechtSanders.WinLibs.*` в этом окружении зависает на 0%
(обновление источника пакетов молча висит без сети и CPU, а не падает с ошибкой).

Тесты — обычный (не IDF) CMake-проект, cJSON берётся из `$IDF_PATH/components/json/cJSON`,
поэтому `export.ps1` должен быть активирован перед конфигурацией (нужен `IDF_PATH`).

⚠️ `IDF_PATH` от `export.ps1` — с обратными слэшами (`C:\esp\esp-idf`). Подставленный
как есть в CMake-строку, `\e` из «`\e`sp-idf» ловится как невалидный escape и валит
конфигурацию. Лечится `file(TO_CMAKE_PATH ...)`, а не голой подстановкой.

⚠️ `ctest.exe` не в PATH этого окружения — он рядом с `cmake.exe` в
`C:\Users\labax-home\.espressif\tools\cmake\3.30.2\bin\`.

## Принятые архитектурные решения

1. **Нативный ESP-IDF, не Arduino и не Tasmota.** Tasmota+Berry+HASPmota рассмотрена и
   отклонена: мы строим свой лончер, свой манифест, свою систему биндов и свой протокол
   с ПК-агентом — в Tasmota всё это уже есть и всё чужое, пришлось бы бороться по всем
   четырём пунктам. Оставлена как источник идей по декларативному UI.
   MicroHydra и ESP-Brookesia отклонены: рассчитаны на ESP32-S3 с PSRAM.
2. **UI на LVGL 9** + `esp_lvgl_port` (только для дисплея и LVGL-таска; indev'ы регистрируем
   сами, т.к. тач у нас bitbang).
3. **Раскладка по ядрам:** WiFi/TLS на core 0, LVGL на core 1. Без PSRAM это единственный
   способ убрать подлагивания UI.
4. **Приложения живут на microSD**, не во flash. Причина — 4 МБ flash и двойной OTA почти
   не оставляют места. Шрифты и картинки тоже на SD, иначе не влезем в слот прошивки.

   ⚠️ **Уточнено 20.08.2026**: считаем, что карта в устройстве есть **по умолчанию**
   (штатная комплектация, не экзотика) — это решение про продукт, не про физику флеша,
   и держаться за «работает без карты» там, где это не даёт реальной пользы, смысла
   больше нет. Но сами ассеты (иконки, картинки, анимации) всё равно остаются на SD,
   не в прошивке — это отдельный вопрос, чисто про место: слот на момент этой записи
   занят на ~91% (свободно ~150 КБ), и каждый килобайт ассета в прошивке — вес,
   который придётся качать по OTA при каждом обновлении, тогда как карта рядом создана
   ровно для такого контента. Один билд прошивки, не два: элемент интерфейса при
   инициализации проверяет, есть ли на карте файл-ассет, если да — грузит его, если
   нет — использует встроенную векторную/глиф-заглушку (тот же приём деградации, что
   уже применяется для `APP_CAP_HOST` и для реестра приложений без карты вообще).
   Отдельные sdkconfig-профили «standard/extended» рассмотрены и отклонены — двойная
   цена тестирования и OTA-путаницы (какой `.bin` куда шить) ради того, что рантайм-
   проверка даёт бесплатно.
5. **Двухслойная модель приложения:**
   - *Слой A (декларативный)* — `ui.jsonl` (layout в стиле openHASP) + описание источника
     данных (HTTP + путь к полю в JSON + период опроса). Без исполнения кода на устройстве.
     Закрывает погоду/курсы/табло — большинство виджетов. **Начинаем с него.**
   - *Слой B (скриптовый)* — Lua, только для виджетов с логикой (напоминания, помидор).
     Добавляем когда упрёмся в слой A.
6. **Двойной OTA с первого дня** — менять разметку потом означает полный реflash.
7. **Автономность обязательна.** Устройство должно работать при выключенном/спящем ПК.
   Каждое приложение объявляет capabilities (`net.http`, `time`, `storage`, `host`, `notify`).
   Виджеты без `host` обязаны быть полностью функциональны без ПК. Виджеты с `host` при
   отсутствии агента показывают последнее значение + отметку давности, не исчезают и не падают.
   **Напоминания живут на устройстве** (расписание в NVS/на SD, свой планировщик,
   сигнал через RGB-LED + динамик) — иначе главная фича умрёт вместе с компом.
8. **Тонкий HAL с первого дня.** У пользователя есть ESP32-C3 SuperMini как возможная
   вторая цель/спутник. Расплата за абстракцию нулевая, поэтому железо — только через BSP.

## Кнопки и комбинации

Пользователь хочет биндить действия на **комбинации** кнопок. Вариант A: 3 кнопки напрямую.

| Кнопка | Пин | Режим |
|---|---|---|
| B1 | GPIO22 | внутренний pull-up, кнопка на GND |
| B2 | GPIO27 | внутренний pull-up, кнопка на GND |
| B3 | GPIO35 | **только вход, внутренних pull-up нет** → внешний 10 кОм на 3V3 |

3 кнопки → 7 непустых масок × (короткое/длинное) = 14 действий + двойной клик + автоповтор.

**Окно разрешения хорда 50 мс** — нельзя выдавать событие сразу по нажатию, иначе `B1+B2`
всегда распознается как `B1`, затем `B2`. Опрос с антидребезгом 5 мс, накопление маски,
по истечении окна — фиксация. Цена: 50 мс задержки, для навигации незаметно.

Системные бинды (перехватывает оболочка, приложение их не видит):
`B1+B3` короткое = домой/лончер, `B1+B2+B3` длинное = настройки /
аварийный выход из зависшего виджета (обязательно — иначе кривой виджет запирает устройство).
Яркость системным биндом на кнопках больше не управляется — убрана 20.08.2026
(ROADMAP #31), теперь только слайдер в Settings → Display, тач.
Остальное отдаётся активному приложению; манифест объявляет желаемые бинды, конфликты
разрешает оболочка (система всегда выигрывает). Таблица биндов — JSON на SD / в NVS,
пользователь переназначает без перепрошивки.

### Фактическая раскладка: РАСПАЯНЫ ДВЕ КНОПКИ (B1, B2 на CN1)

`input_set_present_mask(INPUT_B1 | INPUT_B2)` в `main.c`. GPIO35 объявлен
отсутствующим и **не опрашивается вообще** — без внешних 10 кОм он бы плавал и
устройство жало бы себе B3 само.

Раскладка выбирается по маске в рантайме (`three_buttons()` в `shell.c`), потому что
без B3 нет отдельной клавиши «выбрать», и это сдвигает сразу несколько биндов:

| Действие | 2 кнопки | 3 кнопки |
|---|---|---|
| вверх / вниз | `B1` / `B2` | `B1` / `B2` |
| выбрать | **`B1+B2` коротко** | `B3` коротко |
| домой | `B1` длинно | `B1` длинно или `B1+B3` |
| аварийный выход | `B1+B2` длинно | все три длинно |

`B1` длинно = «домой» работает в **обеих** раскладках намеренно: это единственный
путь назад, доступный резистивному тачу, который однопальцевый и хорд не умеет.

### ⚠️ COM-порт НЕ фиксирован

Был COM13, после переподключения стал **COM4**. Windows раздаёт номер по USB-гнезду.
Скрипты определяют порт по имени устройства `USB-SERIAL CH340 (COMxx)`, а не по числу.

### ⚠️ RTS держит плату в ресете

RTS заведён на EN. Если закрыть COM-порт, не сняв RTS, чип остаётся в ресете, и
следующая прошивка падает с `No serial data received` **на исправном порту**.
Любой скрипт, трогающий последовательный порт, обязан перед закрытием сделать
`setRTS(False)` и `setDTR(False)`. Лечится переподключением USB.

Не сцеплять прошивку и чтение лога в одну команду без паузы — предыдущий читатель
ещё держит порт, и прошивка падает с `port is busy`.

### Состояние железа кнопок (историческое): БЫЛИ НЕ РАСПАЯНЫ

Пользователь хочет сначала сборку без кнопок. Паяльник, кнопки и резисторы есть;
в комплекте платы был **один 4-проводный JST-пигтейл**.

⚠️ **Тач резистивный (XPT2046) → одно касание, мультитач физически невозможен.**
Значит хорды через тач протестировать нельзя. Поэтому входной слой имеет три
взаимозаменяемых бэкенда, оболочка их не различает:
1. `gpio` — боевой, 3 кнопки (после пайки)
2. `touch_zones` — 3 зоны внизу экрана, **только одиночные** нажатия и long-press
3. `debug_inject` — синтетические события с ПК (serial/WS), **умеет любую маску** → хорды
   отлаживаем им до пайки

Первый шаг пайки, если понадобится: пигтейл на **CN1** (`GND · GPIO22 · GPIO27 · 3V3`) →
2 кнопки на внутренних pull-up, **без единого резистора**. Третья (GPIO35) потребует
разъём **P3** + внешний 10 кОм.

## Разметка flash (4 МБ)

См. `partitions.csv`. Даёт 1664 КБ на слот прошивки (двойной OTA с откатом) +
576 КБ LittleFS под конфиг, кэш и таблицу биндов. Остальное — SD.
1664 КБ под LVGL + WiFi + TLS + Lua тесновато; держать шрифты и графику на SD.

## Разрешённые версии зависимостей (component manager, зафиксированы в dependencies.lock)

| Компонент | Версия |
|---|---|
| `lvgl/lvgl` | **9.5.0** |
| `espressif/esp_lvgl_port` | 2.9.0 |
| `espressif/esp_lcd_ili9341` | 2.0.2 |
| `joltwallet/littlefs` | 1.22.3 |
| `espressif/cmake_utilities` | 0.5.3 |
| `espressif/mdns` | 1.11.3 |
| `espressif/esp_websocket_client` | 1.8.0 |

⚠️ `mdns` в ESP-IDF 5.5.5 не входит в ядро (в `components/` внутри самого IDF
его нет) — начиная с какой-то версии IDF он выделен в component registry,
подключается через `idf_component.yml`, как и остальные пакеты в этой
таблице.

## Замеры

Первая успешная сборка: `deskos.bin` = **0x9C4C0 (625 КБ)**, слот 0x1A0000 (1664 КБ),
**свободно 62%**.

⚠️ Цифра **не финальная**: WiFi, TLS/mbedTLS, HTTP-клиент и Lua ещё не подключены.
Именно они и займут основной объём. Перемерить после появления сети.

### ⚠️ Дисплей: ST7789, а НЕ ILI9341 (выяснено на стенде, стоило ~7 итераций)

Плата продаётся как ESP32-2432S028**R** с ILI9341, но панель здесь на **ST7789**.
Подтверждено дважды: экспериментом и отзывами к товару. Наборы команд перекрываются
настолько, что чужой драйвер «почти работает» — и именно это сбивает с толку.

Итоговая рабочая конфигурация (проверена фотографией экрана: рамка сомкнута по всем
четырём краям, диагональ из угла в угол прямая):

| Параметр | Значение | Как выяснено |
|---|---|---|
| Драйвер | `esp_lcd_new_panel_st7789` | под ILI9341 адресное окно заворачивалось: неполная заливка, дублирование маркеров, полосы |
| Ориентация | `swap_xy=1, mirror_x=1, mirror_y=0` | перебор 5 комбинаций; правильная = поворот на 180° от той, что видел пользователь |
| Порядок каналов | **RGB**, не BGR | под BGR красный и синий менялись местами. Большинство материалов по CYD утверждает BGR — для этой партии неверно |
| Порядок байт | `swap_bytes = true` в `esp_lvgl_port` | LVGL кладёт RGB565 как little-endian uint16, панель ждёт старший байт первым |
| Частота SPI | **20 МГц** (проверено) | 40 МГц не проверялась под ST7789; поднимать только глядя на разрывы кадра |
| `x_gap`/`y_gap` | **не нужны** | рамка касается всех краёв, смещения нет |

Диагностика осталась в `main.c` под `#define DESKOS_DISPLAY_DIAG` — перебор драйверов,
ориентаций, цветовых кодировок и чёрно-белый тест геометрии. Включается одной цифрой.

⚠️ **Разворот задаётся В ДВУХ МЕСТАХ и они обязаны совпадать:** `bsp_display_init(landscape=true)`
и поле `.rotation` в `lvgl_port_display_cfg_t` (`shell.c`). Причина: `lvgl_port_add_disp`
**сам вызывает** `esp_lcd_panel_swap_xy`/`mirror` со своими значениями и затирает то, что
выставил BSP. Оставишь `.rotation` по умолчанию (всё false) — панель вернётся в портрет
240×320, а LVGL продолжит считать экран 320×240: интерфейс выйдет повёрнутым на 90° с
остатками старого кадра в незакрытой области. «Двойного разворота» бояться не нужно —
это абсолютные сеттеры, а не относительные повороты.

Меняешь ориентацию — правь **оба** места одновременно.

**Грабли, на которые я наступил (не повторять):**

1. **`max_transfer_sz` в конфиге SPI-шины ограничивает ОДИН `draw_bitmap`, а не только
   flush из LVGL.** Передача сверх лимита **отвергается целиком**, и узнать об этом можно
   только по коду возврата. Я его не проверял → два теста подряд молча показывали остатки
   предыдущего кадра, и я искал причину совсем не там. Теперь лимит с запасом (80 строк),
   а `draw_bitmap` проверяется.
2. **Нельзя диагностировать геометрию по цветам, пока сама кодировка цвета под вопросом.**
   Разделять: геометрию проверять чёрно-белым (эти два значения одинаковы при любом
   порядке байт и каналов), цвет — отдельно, на заведомо верной геометрии.
3. **Словесное описание экрана — плохой канал передачи данных.** Семь итераций ушло на то,
   что одна фотография закрыла за минуту. Просить фото сразу.
4. **«Бледное, засвеченное» было ДВУМЯ разными проблемами** — перепутанными каналами и
   засветкой подсветки сквозь чёрный пиксель. Второе не лечится драйвером: у панели
   слабый чёрный, на 100% яркости тёмная тема выглядит блёкло-синей. Дефолт снижен до 55%.

### Проверено на живой плате (первая прошивка, 17.08.2026)

Разметка применилась, устройство загружается и работает:

| Замер | Значение |
|---|---|
| Free heap до инициализации | 243 624 Б |
| **Free heap после полной загрузки** | **176 128 Б (172 КБ)** |
| Стоимость LVGL + дисплей + тач + FS | ~66 КБ |

⚠️ 172 КБ — это **весь оставшийся бюджет RAM** на WiFi (~50–60 КБ), TLS и приложения.
Без PSRAM это главное ограничение проекта. Каждый новый буфер считать.

Хорд-движок **проверен на железе**, все 7 сценариев декодированы верно, включая
двойной клик, длинное нажатие с автоповтором и аварийный `B1+B2+B3` long.
Отсутствие microSD обрабатывается корректно: `sdmmc_card_init failed (0x107)` →
предупреждение → оболочка поднимается и сообщает об отсутствии карты.

Три ловушки Kconfig, уже пойманные (не наступать снова):
- **`sdkconfig.defaults` не применяется к уже существующему `sdkconfig`.** Добавили
  новый символ в defaults — надо удалить `sdkconfig` и пересобрать, иначе правка
  молча не подействует. Именно так впервые не сработал `SPI_FLASH_SUPPORT_BOYA_CHIP`.
- `CONFIG_FATFS_CODEPAGE_1251` **не существует** в IDF. Кириллических вариантов
  только OEM-866; Windows-1251 нет. Нам не нужно — имена папок приложений латиница.
- `CONFIG_ESP_SYSTEM_PANIC_*` — это Kconfig `choice`. Член choice нельзя выставить
  в `n`, выбирается другой член в `y`. Оставлено по умолчанию (print + reboot).
- `sdkconfig` хранит `CONFIG_DESKOS_TZ` как явное значение, а не ссылку на дефолт
  из `Kconfig.projbuild` — правка дефолта (`EET-2EEST,...`, София/Пловдив) не
  подтягивается в уже сгенерированный `sdkconfig`. На живой плате там оказался
  `UTC0` (видимо, выставлен вручную при более раннем menuconfig) — часы виджета
  на 3 часа отставали от реального времени, хотя SNTP синхронизировался исправно.
  Чинится правкой `sdkconfig` напрямую (он не в git) под фактический пояс устройства.

## Компоненты

```
bsp       железо: дисплей, тач, SD, LED. Единственное место с номерами пинов
input     кнопки, хорды, три бэкенда ввода
registry  реестр приложений. ОТДЕЛЬНЫЙ компонент: нужен и shell'у, и widget'у,
          внутри любого из них дал бы циклическую зависимость
widget    рантайм слоя A: разбор ui.jsonl (label/rect/line/bar/arc/image),
          HTTP- и локальные источники (clock, host), подстановка {{path}}
net       WiFi STA + SNTP, автопереподключение с backoff
host      клиент ПК-агента: mDNS + WebSocket, протокол в docs/host-protocol.md
shell     лончер, статус-бар, системные комбинации
```

### Встроенные приложения распаковываются, а не компилируются

`main/builtin/*` вшиты через `EMBED_TXTFILES` и при старте выкладываются в
`/fs/apps/<id>/`. Можно было бы рисовать их вкомпиленным C, но тогда путей запуска
виджета стало бы два, а проверялся бы только один. Так загрузчик один — файловый,
и устройство полезно ещё до появления microSD.

Реестр сканирует **два корня**: `/fs/apps` (встроенные) и `/sd/apps` (пользовательские).

⚠️ `EMBED_TXTFILES` дописывает NUL, и символ `_end` его считает. Длина = разность
минус 1, причём вычитать надо **из разности**: `_end - 1` компилятор читает как
индекс −1 внешнего массива и валит сборку по `-Werror=array-bounds`. См. `EMB_LEN`.

### Учёт памяти после подключения WiFi

| Замер | Было (без сети) | Стало |
|---|---|---|
| `deskos.bin` | 625 КБ | **1428 КБ из 1664 КБ (свободно 15%)** |
| Free heap после загрузки | 175 864 Б | **125 340 Б** |
| Free heap с поднятым WiFi | — | **80 440 Б** |

⚠️ 80 КБ — это реальный остаток при подключённой сети. Рукопожатие TLS даёт пик
~40 КБ, то есть запас примерно двукратный. Каждый новый буфер считать.

⚠️ **Флеш стал узким местом.** WiFi + mbedTLS + полный bundle сертификатов дали
+750 КБ. На слой B (Lua, ~200 КБ) остаётся 282 КБ — влезет впритык.
Чем добирать при нехватке: `CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_CMN` вместо
полного бандла (~40 КБ), отключение неиспользуемых шифров mbedTLS.

Heap: 125 КБ, из них рукопожатие TLS съедает пик ~40 КБ. Поэтому буфер LVGL
уменьшен с 40 строк до 24 (30 КБ на две буферы вместо 50).

### Учётные данные WiFi

Приоритет: **`/sd/wifi.json`** → NVS → `CONFIG_DESKOS_WIFI_SSID`/`_PASSWORD` из
`sdkconfig`. Файл на карте, если есть, побеждает всегда — это самый быстрый
способ сменить сеть или отдать устройство другому человеку: переложить карту,
без пересборки и без похода в NVS. Формат — `{"ssid":"...","password":"..."}`,
`password` необязателен (открытая сеть). Невалидный JSON или отсутствующий
`ssid` — предупреждение в лог, откат на следующий уровень приоритета.

`sdkconfig` **не** в git, поэтому пароль туда не утекает в репозиторий.
Пароль в лог не пишется никогда ни на одном из трёх путей. Рантайм-путь смены
NVS-учётки — `net_set_credentials()` (пока без UI на устройстве).

## Статус: сквозной путь работает

Проверено на живой плате 18.08.2026 — устройство загружается, показывает лончер,
листается кнопками, открывает виджет, тянет данные из интернета и рисует их:

- дисплей ST7789 320×240, ориентация и цвета верные
- microSD `SA02G 1882MB` монтируется, `/sd/apps` сканируется
- кнопки B1/B2 на CN1, хорды декодируются
- WiFi ассоциируется, SNTP выставляет часы по Europe/Sofia
- оба встроенных виджета в реестре, слой A отрисовывает погоду Пловдива

Проверено 18.08.2026: встроенный виджет **`clock`** (ROADMAP #1) — локальный
источник `{"src":"clock"}`, показывает время/дату/день недели, тикает раз в
секунду, не зависит от HTTP. Заодно всплыла и починена рассинхронизация
`sdkconfig` с дефолтным часовым поясом (см. «Три ловушки Kconfig»).

Проверено 18.08.2026: **ROADMAP #3** (`bar`/`arc`/`line`, `image` без иконки не
проверялся) и **#4** (пайплайн фильтров `{{path | filter:arg}}` — round/
арифметика/`dict`/`date`, плюс условная видимость `"if"`). Погодный виджет
показывает `Cloudy` вместо `code 3` через словарь WMO-кодов.

Спроектировано 18.08.2026 (ROADMAP #6): навигация оболочки — Home (полноэкранный
закреплённый виджет) / Full list (сетка плашек, Settings внутри неё же) /
Settings (нативный экран, там же чек-лист закрепления). Глобальный тулбар
(часы + иконки) — на `lv_layer_top()`, поверх всех экранов, включая открытый
виджет; авторам `ui.jsonl` резервировать верхние ~16px. Подробности —
[docs/shell-navigation.md](docs/shell-navigation.md).

Проверено на живой плате 18.08.2026: **ROADMAP #7**, но узким срезом —
`components/shell/settings.c` реализован на текущем плоском списке лончера
(сетка/Home/тулбар из #6 сознательно отложены отдельным проходом). Яркость и
часовой пояс (пресеты) меняются с устройства через NVS и переживают
перезагрузку; чек-лист закрепления на Home тоже в NVS. **Визуал временный**,
редизайн — отдельная сессия, механику менять не должно.

Сделано 19.08.2026 (ROADMAP #8): `net_init()` читает `/sd/wifi.json` раньше
NVS и `sdkconfig` (собрано, на живой плате отдельно не проверялось). Плюс
экран **Settings → WiFi → Configure** — временная точка `deskos-setup` +
HTML-форма на 192.168.4.1, без клавиатуры на устройстве. Проверено на живой
плате: полный цикл телефон → форма → сохранение → возврат в STA прошёл
чисто на сети `MERCUSYS_97B4`.

По пути нашёлся и починен реальный баг: при возврате AP→STA код и
`net_softap_stop()`, и обработчик `WIFI_EVENT_STA_START` оба вызывали
`esp_wifi_connect()` — гонка ловилась как `wifi:sta is connecting, return
error`, дальше reconnect не срабатывал вообще, устройство зацикливалось в
бэкоффе. Починено: `esp_wifi_connect()` теперь вызывается ровно один раз,
из `STA_START`, как при обычной загрузке. Первая попытка проверки ошибочно
взяла сеть `labax_5G` — этот ESP32 не поддерживает 5 ГГц вообще, симптом на
экране был неотличим от бага (тот же бесконечный бэкофф), стоит помнить при
диагностике похожих жалоб позже.

Стоимость прошивке — SoftAP-портал (`esp_http_server` + обвязка) добавил
**~20 КБ**: 1428→1451 КБ (`/sd/wifi.json`, около нуля) →1472 КБ (SoftAP),
свободно осталось **~226 КБ (14%)** из 1664 КБ слота. Бюджет под будущий Lua
(ROADMAP #21, ~200 КБ) всё ещё сходится, но запас стал заметно тоньше —
подрезка bundle mbedTLS (~40 КБ) остаётся в кармане на случай, если понадобится
глубже.

Сделано 19.08.2026 (ROADMAP #9): `widget.c` считает физические номера строк
`ui.jsonl` и копит проблемы разбора (битый JSON, отсутствующий `obj`,
неизвестный тип вида, переполнение лимита видов) в жёлтый баннер на самом
экране виджета — раньше всё это молча уходило в лог, которого пользователь
не видит. Отдельно: опечатка в `entry` манифеста (файл не открылся вообще)
теперь тоже рисует экран с текстом ошибки вместо того, чтобы нажатие в
лончере просто ничего не делало. Проверено на живой плате: временно испортил
`hello.ui.jsonl` тремя намеренными ошибками — баннер с точными номерами строк
появился на экране, подтверждено пользователем, файл возвращён к исходному
состоянию.

Сделано 19.08.2026 (ROADMAP #10): виджет может опрашивать несколько
`http`-источников с разными периодами и пространствами имён —
`{"src":"http","url":"...","as":"NAME","every":N}` кладёт тело под
`{{NAME.путь}}`, источник без `as` по-прежнему расстилается на верхний
уровень (старые `ui.jsonl` не трогать). Планировщик — один таск с секундным
тиком вместо одного большого периода сна; побочный фикс — статус
`waiting for network` теперь перепроверяется каждую секунду, а не раз в
весь период (было можно застрять на 900с). Добавлен индикатор `loading` в
статусной строке виджета сразу при открытии — раньше угол был пуст до
первого ответа (до ~10с при таймауте TLS), неотличимо от «нечего
показывать».

Проверено на живой плате: временно добавил в `hello` два именованных
источника (погода + курс USD/EUR) с разными периодами, пользователь
последовательно увидел все три состояния (только погода, только курс, оба
сразу) — источники фетчатся независимо, сбой одного не блокирует другой.
По пути всплыли два внешних сюрприза: `api.frankfurter.app` сейчас редиректит
на `.dev` (наш клиент редиректы не разворачивает), `api.open-meteo.com`
дважды не уложился в 10-секундный TLS-таймаут на этой сети — оба не баги
кода. Тестовые строки убраны после проверки.

Сделано 19.08.2026 (ROADMAP #11): виджет пишет объединённый документ всех
своих источников в `<dir>/.cache.json` (`{"ts":epoch,"data":{...}}`) при
каждом успешном фетче и читает его синхронно в `widget_open()` — экран
получает вчерашние данные ДО первой попытки сети, а не после её тайм-аута.
Статус честно показывает возраст (`cached Nm/h/d ago`) или просто `cached`,
если время ещё не синхронизировано.

⚠️ Поймал на этом реальный краш: буфер чтения кеша (4 КБ) был на стеке
`cache_load()`, а тот вызывается из LVGL-таска с стеком всего 6144 байта
(`shell.c`) — переполнение стека портило heap где-то по соседству,
проявляясь как `assert failed: remove_free_block ... prev_free field can
not be null` при следующей же аллокации, то есть симптом всплыл не в месте
причины. Починено переносом на heap (`malloc`/`free`) — тот же паттерн уже
был в этом файле для куда большего буфера `ui.jsonl`, стоило сразу сверить
с ним. Урок на будущее: **любой локальный буфер на несколько КБ в коде,
который может исполняться на LVGL-таске — сразу heap, стек только для
мелочи.**

Проверено на живой плате (после фикса): `weather` записал кеш после первого
фетча, при повторном открытии подхватил его с диска за 7мс до прихода живых
данных, экран не падал. Не проверен вживую полный сценарий «перезагрузка без
сети» — риск минимален (чтение кеша не зависит от состояния сети), но честно
не пройден.

Сделано 19.08.2026 (ROADMAP #13): новый компонент `components/host` —
клиент ПК-агента по протоколу из #12. Один общий фоновый supervisor-таск
(не по соединению на виджет), перечитывает `/sd/agent.json` каждые 3с
(токен подхватывается без перезагрузки), ищет агента через
`mdns_query_ptr()`, дальше — `esp_websocket_client` с его встроенным
авто-реконнектом; своя логика нужна только чтобы сбросить закешированный
адрес после 5 подряд неудач или явной ошибки от агента. Новый источник
`{"src":"host","topic":"..."}` в `widget.c` подписывает/отписывает
клиента при открытии/закрытии виджета, входящие `data` идут в тот же
`merge_source()`, что уже обслуживает именованные HTTP-источники (№10).

⚠️ Задача заодно вскрыла, что мутация `s_combined` в `widget.c` была
защищена только явочным порядком (единственный писатель — `refresh_task`),
а не логом — с появлением второго писателя (колбэк `host.c` на своём
таске) это стало настоящей гонкой. Почина: сетевой фетч — до захвата
LVGL-лока, `merge_source()`/`apply_data()` — строго под ним; запись кеша
на диск (№11) осталась вне лока, под лок ушла только сериализация в
строку.

Проверено на живой плате: временный Python-скрипт-заглушка (не настоящий
агент — это №15) с mDNS-анонсом и WebSocket-сервером на этом же ПК.
Устройство само нашло его и обменялось `hello`/`hello_ack` без
перезагрузки; тестовый `{"src":"host","topic":"cpu"}` в `hello` показал
живое обновление значения на экране каждые 4с синхронно с пушами
заглушки. Стоимость флешу — mDNS + `esp_websocket_client` вместе
**~3% слота** (13%→10% свободно); heap после загрузки просел с ~78 КБ до
**~65 КБ** уже просто от инициализации, без активного соединения —
mDNS-таск и структуры WS-клиента стоят памяти всегда. Тестовые файлы и
правки `hello.ui.jsonl` убраны после проверки.

Сделано 19.08.2026 (ROADMAP #15, узким срезом): настоящий агент под
Windows — [agent/](agent/), C#/.NET 10, WinForms-трей без окна. Тема
одна — `cpu` (проценты загрузки, температура пока всегда `null`).
Обнаружение — mDNS (`Makaretu.Dns.Multicast`), сервер — ручной
WebSocket-handshake поверх `TcpListener` (не `HttpListener`: тому нужны
admin-права или ручной `netsh` под wildcard-адрес, плохой первый запуск
для трей-агента). Токен генерируется сам при первом запуске
(`%LOCALAPPDATA%\DeskosAgent\config.json`), автозапуск — через реестр
`HKCU\...\Run`, без установщика.

⚠️ Загрузку CPU считал сначала через `System.Diagnostics.PerformanceCounter`
— упало на этой же машине сразу при старте (`Cannot load Counter Name
data... performance counters may need to be repaired`, известная беда
реестра PerfLib). Переписано на `GetSystemTimes` (P/Invoke) — от него не
зависит. Стоит помнить при похожих сборках метрик под Windows: PerfLib
не гарантированно исправен на произвольной машине.

Проверено на живой плате: устройство нашло агента через mDNS, прошло
рукопожатие, подписалось на `cpu` и показывало настоящую загрузку
процессора этого ПК в реальном времени, с заметной реакцией на нагрузку
машины. Полный список того, что сознательно не сделано (температура,
`now_playing`/`build_status` из №14, установщик) — в
[agent/README.md](agent/README.md) и ROADMAP #15.

Сделано 19.08.2026 (ROADMAP #14, узким срезом): встроенное приложение
**`system`** — дуга + число, `{"src":"host","topic":"cpu"}`. Только `cpu`,
других тем агент (№15) пока не отдаёт. По пути нашёлся и починен реальный
баг: `datasource_truthy()` считала JSON `null` истиной (ни одна из веток
не подходила, падало в `return true` в конце) — не всплывало раньше,
потому что до host-протокола ни один источник не присылал буквальный
`null` (HTTP API просто опускают поле). Актуально для будущей температуры
(`temp_c` в протоколе намеренно `null`, пока агент её не считает) — без
фикса `{"if":"cpu.temp_c"}` не спрятал бы пустое значение. Тест добавлен
в `tests/datasource/`.

Сделано 19.08.2026 (ROADMAP #16, узким срезом): поле `bindings` в манифесте
работает, но действие пока одно — `refresh` (форсировать повторный запрос
http-источников виджета в обход `every`). `input_parse_event_str()` —
обратная функция к `input_event_str()` (та с самого начала была подписана
«for the bindings editor UI», просто ждала своего часа), разбирает строки
вида `"B1+B2 click"`. Конфликты с системными комбинациями не нужно
разруливать отдельной таблицей — проверка биндов виджета в `shell.c` стоит
физически после `handle_system_chord()` в том же цикле: занятая системой
комбинация просто никогда туда не доходит. Добавлен индикатор
`refreshing...` в углу виджета сразу по нажатию — без него нажатие ничем
не отличалось от «ничего не произошло» до завершения сетевого запроса.

⚠️ Таблица переназначений пользователя в NVS (из исходной задачи) — не
сделана, самостоятельная задача уровня экрана калибровки тача (№5, нужен
интерактивный «нажмите новую комбинацию»). Другие действия для `bindings`,
кроме `refresh`, — заводить по мере реальной надобности, не заранее.

Проверено на живой плате: `weather` получил постоянный `"bindings":
{"B1+B2 click":"refresh"}`, нажатие логировалось и немедленно запускало
HTTP-попытку независимо от 900-секундного периода, `refreshing...`
подтверждён визуально. Попутно всплыла отдельная от этой задачи проблема
— `api.open-meteo.com` стабильно падает по TLS (`PK verify failed`,
сертификат/бандл CA) — не логика обновления, разбираться решили отдельно.

Сделано 19.08.2026 (ROADMAP #17, узким срезом): новый компонент
`components/ota` — OTA-обновление вручную из Settings. `/sd/ota.json`
(`{"url":"http://host/deskos.bin"}`, та же схема, что `wifi.json`/
`agent.json`) читается фоновой задачей, которая гоняет
`esp_https_ota_begin/perform/finish` и репортит прогресс колбэком
(`connecting`/`downloading NN%`/`done`/`error`) в новую кнопку «Check for
update» + строку статуса на экране настроек. `main.c` вызывает
`esp_ota_mark_app_valid_cancel_rollback()` после старта — откат при
неудачной прошивке отдан целиком бутлоадеру
(`CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE` стоял в `sdkconfig.defaults` с
первого дня проекта, здесь впервые действительно используется).

⚠️ Реальный use-after-free найден и починен до прошивки, не на железе:
OTA-задача продолжает жить и слать прогресс в колбэк независимо от того,
открыт ли ещё экран настроек — `s_ota_status_label` теперь обнуляется в
`settings_close()`, иначе следующий колбэк писал бы в чужой/удалённый
LVGL-объект.

Проверено на живой плате: текущая сборка прошита как «база», локальный
`python -m http.server` на ПК раздал `build/deskos.bin`, `/sd/ota.json`
записан через картридер (карта туда-обратно, устройство не трогали
разобранным). «Check for update» из Settings — лог подтвердил `esp_https_ota`
пишет в `<ota_1>` at offset `0x1b0000`, ~36с скачивания по локальной сети,
`esp_restart()`, следующий boot-лог — `Loaded app from partition at offset
0x1b0000`. WiFi и оболочка поднялись чисто на новом слоте. Раздел
переключается по-настоящему, откат бутлоадера не проверялся отдельно
(для этого нужен намеренно битый образ — не делали).

⚠️ Проверка образа — то, что даёт `esp_https_ota` из коробки (заголовок +
опциональная TLS-цепочка), без отдельной подписи. Версия не сверяется —
обновление в тесте выполнилось, даже когда `current`/`new` версия в логе
совпали дословно (`4f95279-dirty` -> `4f95279-dirty`). Проверка по
расписанию не делается, только ручная кнопка.

⚠️ Флеш: `components/ota` + `esp_https_ota`/`app_update` добавили
некритичный прирост — свободного места в слоте прошивки на момент
проверки **~9% (~150 КБ)**, было 14% на момент №8. Бюджет под слой B
(Lua, ROADMAP #21, нужно ~200 КБ) продолжает сжиматься с каждой сетевой
фичей — подрезка `CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_CMN` вместо
полного бандла CA (~40 КБ) остаётся главным резервом, если понадобится.

Сделано 19.08.2026 (вне номеров ROADMAP, по просьбе пользователя): экран
Settings переведён с плоского списка на разделы — **About device** (место
под логотип — закрашенный кружок-заглушка, имя устройства из
`esp_app_get_description()->project_name`, версия прошивки, «Check for
update»), **Display** (яркость), **Network** (часовой пояс, WiFi),
**Storage** (карта — статус/имя/объём через новые `bsp_sd_info()`, внутренний
LittleFS — новый `bsp_fs_usage()`, свободный heap), **Apps** (чек-лист
закрепления на Home, без изменений в логике). Реализовано одним экраном с
подменяемым содержимым (`lv_obj_clean(s_list)` + перестройка на каждый
переход) и общей кнопкой «Back» первой строкой в каждом разделе — второй
навигационный уровень поверх уже существующего «B1 длинно = всегда домой».

⚠️ Реальный баг найден и починен по итогам первой же проверки на живой
плате: чтобы обойти по кругу пункты меню Settings (5 штук), после
последнего требовалось около 5 лишних нажатий «вниз». Причина —
единственная LVGL-группа фокуса (`shell_input_group()`) была общей на весь
проект: кнопки лончера (4 приложения + Settings = ровно 5) оставались
зарегистрированы в ней даже когда экран лончера не активен, и навигация
докручивала их вхолостую перед возвратом к первому пункту Settings. Чинить
сам лончер не пришлось: Settings завёл свою собственную `lv_group_t`,
`shell.c` получил `shell_set_input_group()` для переключения клавиатурного
indev между группами (NULL восстанавливает группу лончера), и правило на
будущее — каждый новый экран вне лончера заводит свою группу вместо
`shell_input_group()`, который теперь документирован как «only the
launcher's own group».

Задеплоено на живую плату **по воздуху** (ROADMAP #17, тот же вечер) — не
по USB: сам факт, что только что проверенный OTA-механизм можно
использовать для настоящего рабочего обновления, а не только для
демонстрации, был первой мыслью пользователя сразу после прошивки этой
сессии по USB. `/sd/ota.json` с прошлой проверки остался на карте
нетронутым, `python -m http.server` в `build/` отдал новый `deskos.bin`,
устройство перезагрузилось на `0x1b0000` без карты в руках — и заодно
доставило фикс фокус-группы. Пользователь подтвердил на экране: разделы
на месте, зацикливание пропало.

Сделано 19.08.2026 (ROADMAP #6, реализация дизайна из docs/shell-navigation.md,
отдельным проходом от самого дизайна и от редизайна Settings в этот же день):
`shell.c` переписан вокруг **Home** (полноэкранный закреплённый виджет,
`B1`/`B2` коротко листают закреплённые, пусто — заглушка) и **Full list**
(сетка тайлов вместо плоского списка, Settings — синтетический тайл в той же
сетке). Переходы без бэкстека: `B1` длинно — всегда Home, откуда угодно, даже
если виджет открыт через Full list, а не как закреплённый на Home. Home и
Full list — каждый со своей группой фокуса; `home_step()` меняет закреплённый
виджет напрямую (`widget_close()`+`widget_open()`), не через LVGL-группу — на
Home нет списка, который можно было бы обходить курсором.

Заодно наконец сделан настоящий глобальный тулбар на `lv_layer_top()`
(часы + `wifi`/`sd`/`pc`/бэкенд ввода, тикает раз в секунду) — до сих пор
статус-строка была просто дочерним объектом лончера и пропадала при открытии
виджета или Settings (сам этот факт был известным дефектом с самого
проектирования №6, просто было её некому чинить, пока не было отдельных
экранов). Новый `shell_set_input_group()` — Settings переключает на него
клавиатурный indev вместо шаринга группы с Full list (без этого — тот же
класс бага, что уже чинили для Settings отдельно, см. запись про раздел
Settings ниже).

⚠️ Два реальных бага найдены на живой плате, не запланированных заранее:
1. Удержание `B1` (домой) или `B2` (яркость) генерирует `LONG_PRESS` один раз
   и затем серию `HOLD_REPEAT` на то же удержание; система глотает только
   `LONG_PRESS`. `HOLD_REPEAT` утекал в новую Home-логику как «переключить
   закреплённый виджет» — держишь `B1`, чтобы выйти домой, а Home тем временем
   спамит `widget_open()` по всем закреплённым, с реальными HTTP-попытками на
   каждом. Почина: Home реагирует только на `CLICK`.
2. `go_home()` считал "мы не дома" по `widget_is_open()` — но на Home виджет
   открыт всегда, это и есть содержимое экрана. Каждое нажатие `B1` длинно на
   Home перезагружало текущий закреплённый виджет впустую. Почина: критерий —
   `s_mode == SHELL_FULL_LIST`, виджет как таковой в эту проверку не входит.

Проверено на живой плате: Home корректно листает `clock`/`weather` короткими
нажатиями, долгое `B1` не спамит `widget_open()`, эскейп-хетч и системные
хорды по-прежнему возвращают домой из любого места. Все правки доставлены
на устройство **по OTA** (№17), USB использовался только для самой первой
прошивки этой сессии.

⚠️ Отдельно найден и починен визуальный баг, который сам новый тулбар и
обнажил: он рисовался прозрачным текстом без фона и сливался с чем угодно
под ним — в первую очередь с заголовками `clock`/`weather` (те кладут свой
хедер прямо в `y:0`, ровно там же, где тулбар). Тулбар стал собственным
непрозрачным `lv_obj` (чёрный фон, высота 18px, `SHELL_TOOLBAR_HEIGHT` в
`shell.h`), все четыре встроенных `ui.jsonl` (`clock`/`weather`/`hello`/
`system`) сдвинуты вниз на те же 18px — это было отложено ещё в самом
дизайн-документе («стоит сдвинуть вниз») и стало реальной проблемой только
теперь, когда тулбар действительно виден поверх виджета. Экран Settings
(список и панель настройки WiFi) тоже не резервировал эти 18px и после
первого фикса тулбара всё ещё прятался под ним — досдвинут отдельно.
Пользователь подтвердил на экране после каждого из трёх фиксов, задеплоенных
по OTA один за другим.

Сделано 20.08.2026 (ROADMAP #30, #31, #18, узкими срезами, одна сессия):

- **Тулбар** (№30): SD-иконка убрана, `wifi`/`pc` — цветные иконки-глифы
  справа вместо текста, `gpio`/`touch`/`inject` слева тоже убран (просил
  пользователь — «ничего не говорящая надпись»).
- **Яркость** (№31): Settings → Display получил `lv_slider` прямо строкой
  списка (не попапом — первая попытка с попапом провалилась на живой плате
  сразу по трём фронтам, см. сам ROADMAP #31 за разбором). Слайдер тянется
  тачем и ведёт себя как обычный пункт меню кнопками: `B1`/`B2` наводятся на
  него как на любую строку, выбор переключает «редактирование» (рамка
  подсветки), в этом режиме `B1`/`B2` уже шагают значение. Заодно убран
  старый системный хот-кей яркости на кнопках (`step_backlight()` в
  shell.c) — стал избыточен.
- **Установка/удаление приложений** (№18): `app_registry_scan()` можно
  вызывать повторно, Settings → Apps — кнопка «Rescan SD card» (со спиннером
  на время сканирования, иначе казалось, что нажатие ничего не делает),
  жёлтые предупреждения о битых манифестах, корзина у SD-приложений (не у
  встроенных — те пересоздаются при каждой загрузке) с диалогом
  подтверждения. Чек-лист закрепления на Home переделан с цветного
  чек-марка на `lv_switch` — чек-марк на фокусе читался как «выбран», белый
  вариант тонул на подсветке фокуса, тогл не требует решений о контрасте.

Отдельно зафиксирован в ROADMAP.md **новый бэклог-пункт №44** — ночной режим
яркости (своё время + своё значение), пожелание пользователя, не
реализовано.

Сделано 21.08.2026 (ROADMAP #32): тогл «Dark theme» в Settings → Display.
Ключевая находка — `sdkconfig.defaults` никогда не задавал
`CONFIG_LV_THEME_DEFAULT_DARK`, то есть все LVGL-дефолтные объекты (кнопки,
переключатели, слайдер, списки, спиннер) весь проект жили на встроенной
**светлой** теме LVGL; «тёмный» вид складывался только из явно
перекрашенного фона экранов (наш локальный стиль, который перебивает тему)
поверх светлых карточек самой темы. Решение — переключать не свою
самодельную палитру, а саму LVGL-тему (`lv_theme_default_init()` повторно
на том же дисплее; она переиспользует те же структуры стилей и репортит
смену, поэтому уже существующие объекты перекрашиваются сами) плюс
перекраска двух мест, где фон задан явно (Home/Full list в `shell.c`,
`s_screen` в `settings.c`). Тулбар и открытые виджеты (`ui.jsonl`) тему
намеренно не наследуют — тулбар оверлей поверх обычно неизменного тёмного
контента виджета, виджеты слоя A красят себя сами построчно, тема их не
касается. Подробности и обе найденные попутные ошибки (нахлёст новой строки
на слайдер яркости, замена акцентного цвета темы с голубого на тёмно-
фиолетовый по пожеланию пользователя — устройство всё-таки Gengar) — в
ROADMAP.md #32.

Проверено на живой плате: тогл переключает Home/Full list/Settings
живьём без перезагрузки, открытый `weather` не меняется вместе с темой
(подтверждено как ожидаемое поведение).

Сделано 21.08.2026 (ROADMAP #37): полоска занятости (`lv_bar` + проценты)
под SD и внутренним LittleFS в Settings → Storage, новый `bsp_sd_usage()`
(через `esp_vfs_fat_info()`, фактическое использование ФС, а не сырая
ёмкость по CSD). По итогам живой проверки почищен весь экран Settings, не
только Storage: снят серый фон под каждой информационной строкой (дефолт
LVGL-темы для `lv_list_add_text`, общая `list_text()` теперь используется
везде вместо голого вызова), убрана строка «SD card: mounted/not present»
(нет карты — экран просто не показывает про неё ничего), «Free heap»
переименован в «Free memory», между разделами Storage добавлены тонкие
разделители. Подтверждено на живой плате.

Репозиторий: https://github.com/labaks/GengarDTB (ветка `main`).

## План работ

Разбит по фазам с критериями готовности в [ROADMAP.md](ROADMAP.md).
Рекомендованный порядок: часы → тесты рендерера на хосте → преобразования
значений в слое A → кеш данных на карте → протокол ПК-агента.

⚠️ **Кириллица отложена по решению пользователя** — русский текст пока не нужен,
а шрифт стоил бы 30–60 КБ флеша, которого и так в обрез. Интерфейс на латинице
**намеренно**, это не недоделка.

## Открытые вопросы

- **TLS к `api.open-meteo.com` падает на живой плате** (найдено 19.08.2026,
  во время проверки ROADMAP #16): `esp-x509-crt-bundle: PK verify failed`,
  `Certificate matched but signature verification failed`,
  `mbedtls_ssl_handshake returned -0x3000`. Время у устройства верное
  (SNTP синхронизирован), значит не банальный «сертификат из будущего».
  Похоже на несовпадение сертификата сайта с бандлом CA
  (`CONFIG_MBEDTLS_CERTIFICATE_BUNDLE_DEFAULT_CMN` вместо полного бандла —
  первый подозреваемый) — не диагностировали дальше, решили разбираться
  отдельной сессией. `api.frankfurter.dev` (проверялся в №10) на этот
  дефект не похож, значит не системная поломка TLS-стека целиком
- ~~Кириллический шрифт~~ — **отложено осознанно**, см. выше. Не «починить при случае»
- **Протокол с ПК-агентом** ([docs/host-protocol.md](docs/host-protocol.md),
  ROADMAP #12) реализован на устройстве (№13, `components/host`), на ПК
  (№15, [agent/](agent/), C#/.NET) и во встроенном виджете `system` (№14) —
  всё проверено вживую друг на друге. Пока только одна тема (`cpu`, без
  температуры) — `now_playing`/`build_status` не начаты, агент их не отдаёт
- **Слой B (Lua)** не начат. На него остаётся ~280 КБ флеша при потребности ~200 КБ —
  влезет впритык, сперва стоит ужать mbedTLS
- **Калибровка тача сделана** (ROADMAP #5, 18.08.2026) — экран калибровки, NVS,
  переживает перезагрузку. Верхний край экрана ловится нестабильно (палец —
  через раз, стилус — почти никогда), остальной экран надёжно; отступ
  калибровочных точек от края не помог, причина не найдена (подозрение на
  ШИМ подсветки). Не докапывались дальше — с физическими кнопками (CN1) тач
  вспомогательный, не основная навигация
- **40 МГц SPI** не проверялась под ST7789, стоит 20
- **Третья кнопка** (GPIO35 + внешние 10 кОм) не распаяна; код к ней готов,
  включается одной строкой `input_set_present_mask` в `main.c`
- Консольный шим для `debug_inject` через UART не сделан (риск конфликта с консолью
  на UART0)
- **`image`-вид (ROADMAP #3) не проверен на железе.** Код есть (`LV_USE_FS_STDIO`,
  буква `A:`, путь `A:<dir app>/<src>`), но нет ни одной иконки в LVGL
  bin-формате (нужен конвертер, напр. lvgl.io/tools/imageconverter) — нечем
  проверить. Отдельная задача ROADMAP #28, не привязана к моменту.

## Что НЕ делать

- Не флэшить, не стирать flash и не менять разметку без явного подтверждения пользователя
  (на плате до сих пор живёт та погодная станция; бэкап есть, но спрашивать всё равно)
- Не публиковать дамп flash — в нём пароль WiFi
- Не открывать COM13 сторонними программами во время работы устройства — DTR/RTS ребутнёт плату
- Не вкомпиливать шрифты/картинки в прошивку — не влезем
- Не тянуть Arduino-совместимость «на всякий случай»
